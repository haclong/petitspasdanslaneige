{% extends "main.njk" %}

{% set posts = collections["post_" + locale] %}

{% block content %}
<div class="container-xl" id="home">
<h1>{{ title }}</h1>
<p>
in src root
{{content | safe}}
<ul>
{%- for post in posts -%}
  <li><a href="{{ post.url }}">{{ post.url }}</a></li>
{%- endfor -%}
</ul>
</p>
</div>
{% endblock %}
